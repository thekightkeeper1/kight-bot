const fs = require('fs');

class Database {
    constructor () {
        /* todo
        *  This class should accept a filename as a parameter, and one which states whether to open or create
        * From here, I think I can basically just have a write operation taht adds a js object to a larger object called db.
        * you can also retrieve and read from it.
        * */
    }

    write
}